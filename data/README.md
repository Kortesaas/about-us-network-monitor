Runtime state lives here (settings, known-device metadata, event history,
inventory override). It is written by the backend and ignored by git. Back it
up with **Settings → Export** or by copying this directory.
